{% extends 'base.html.twig' %}




{% block body %}

    <main class="container">
        <p><h1><strong>Catégories :</strong></h1></p>
        {% for category in categories %}
            <table>
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
            </tr>
        {% endfor %}
        </table>

    </main>

{% endblock %}
